# Copy to .env and fill in to enable the real Claude-powered "Vera" chat.
# Without this the site still works; Vera falls back to the built-in keyword bot.
ANTHROPIC_API_KEY=
# Optional overrides:
# ANTHROPIC_MODEL=claude-sonnet-5
# PORT=8787
